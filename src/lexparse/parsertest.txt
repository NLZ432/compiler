(*
 * We know that n is odd, so just start with 3
 *)

boolean func isPrime(int n) {
  int swag <- 1, swag2 <- 2;
  int yeet <- 0;
  swag, yeet <- 3, 2;
  return false;
}

int func program() {
  return isPrime(1, 2, 3);
}

