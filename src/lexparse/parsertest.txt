(*
 * We know that n is odd, so just start with 3
 *)

extern str func printf(...);

int func getA(str x) {
    printf("%s\n", x);
    return 1;
}

int func program() {
    int a <- 2;
    a <- getA("hii");
    return 0;
}

