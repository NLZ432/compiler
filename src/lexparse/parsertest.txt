(*
 * We know that n is odd, so just start with 3
 *)

extern str func printf(...);

int func program() {
    printf("HI\n");
   
    var a <- 1; 
    while (a <= 10) do { 
        printf("BANGER %d\n", a);
        a <- (a + 1);
    } #swag
   
    return 0;
}

